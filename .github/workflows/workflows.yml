name: k8s-template integration workflow

on:
  pull_request:
    types:
      - opened
      - closed
      - synchronize

jobs:
  kkubernetes-repo-standards:
    uses:
      clubcedille/cedille-workflows/.github/workflows/kubernetes-repo-standards.yml@master
